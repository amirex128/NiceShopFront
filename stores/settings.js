export const useSettingsStore = defineStore('settingsStore', () => {

        const state = reactive({

        })


        return {
            state
        }
    },
    {
        persist: true,
    }
)
